# 📦 Инструкция по добавлению товаров в раздел "НАШ АССОРТИМЕНТ"

## 🎯 Содержание
1. [Способ 1: Через консоль браузера (БЫСТРЫЙ)](#способ-1-через-консоль-браузера)
2. [Способ 2: Редактирование кода (для разработчиков)](#способ-2-редактирование-кода)
3. [Добавление картинок](#добавление-картинок)
4. [Структура товара](#структура-товара)
5. [Примеры](#примеры)
6. [FAQ и решение проблем](#faq-и-решение-проблем)

---

## 📋 Способ 1: Через консоль браузера (БЫСТРЫЙ)

Этот способ НЕ требует знания программирования! Просто следуйте инструкции.

### Шаг 1: Откройте сайт
1. Откройте ваш сайт в браузере (локально или на Render)
2. Нажмите **F12** или **Правая кнопка мыши → "Просмотр кода"**
3. Перейдите на вкладку **"Console"** (Консоль)

### Шаг 2: Добавьте новый товар

Скопируйте этот код и вставьте в консоль, заменив данные на свои:

\`\`\`javascript
// ПРИМЕР: Добавление нового товара
DataManager.addProduct({
    name: 'Средства для бритья',           // Название товара
    category: 'shaving',                   // Категория (латиницей, без пробелов)
    description: 'Пены, станки, бальзамы', // Краткое описание (под названием)
    price: 0,                              // Цена (если нужна, иначе 0)
    icon: '🪒',                            // Эмодзи (вместо картинки)
    image: 'images/shaving.jpg',           // Путь к картинке (или оставьте пустым '')
    visible: true                          // Показывать товар? true = да, false = нет
});

// После добавления - обновите страницу (F5)
location.reload();
\`\`\`

### Шаг 3: Нажмите Enter

✅ Товар добавлен! Страница автоматически перезагрузится.

---

## 🖼️ Добавление картинок

### Вариант A: Через папку /images/ (РЕКОМЕНДУЕТСЯ)

1. Подготовьте картинку (формат: JPG, PNG, WebP)
2. Переименуйте её (например: `shaving.jpg`)
3. Положите файл в папку `C:\Users\你好\Desktop\opt-stock-project\images\`
4. При добавлении товара укажите: `image: 'images/shaving.jpg'`

**После добавления картинки:**
- Сделайте git commit:
  \`\`\`bash
  git add images/shaving.jpg
  git commit -m "Add: Product image for shaving category"
  git push origin main
  \`\`\`
- Render автоматически обновит сайт через 2-3 минуты

### Вариант B: Использовать эмодзи (БЕЗ картинок)

Просто укажите подходящий эмодзи:

\`\`\`javascript
icon: '🪒',  // Бритва
icon: '💄',  // Помада
icon: '🧴',  // Крем
icon: '🧼',  // Мыло
icon: '💅',  // Лак для ногтей
icon: '🌸',  // Цветок (парфюмерия)
icon: '💆‍♀️',  // Уход за волосами
icon: '✨',  // Блеск
icon: '💎',  // Драгоценности (премиум)
\`\`\`

---

## 📊 Структура товара

Каждый товар имеет следующие поля:

| Поле | Тип | Обязательно? | Описание |
|------|-----|--------------|----------|
| **name** | Текст | ✅ Да | Название товара (отображается крупно) |
| **category** | Текст | ✅ Да | Категория латиницей без пробелов (`makeup`, `skincare`) |
| **description** | Текст | ✅ Да | Краткое описание (1 строка под названием) |
| **price** | Число | ⚠️ Опционально | Цена (если нужна) или `0` |
| **icon** | Эмодзи | ✅ Да | Эмодзи для карточки товара |
| **image** | Текст | ⚠️ Опционально | Путь к картинке (`images/file.jpg`) или `''` |
| **visible** | true/false | ✅ Да | Показывать товар на сайте? |

---

## 🎨 Примеры добавления разных товаров

### Пример 1: Товар С картинкой

\`\`\`javascript
DataManager.addProduct({
    name: 'Солнцезащитные кремы',
    category: 'suncare',
    description: 'SPF 30, 50+',
    price: 0,
    icon: '☀️',
    image: 'images/suncare.jpg',  // Картинка есть
    visible: true
});
location.reload();
\`\`\`

### Пример 2: Товар БЕЗ картинки (только эмодзи)

\`\`\`javascript
DataManager.addProduct({
    name: 'Тональные средства',
    category: 'foundation',
    description: 'BB-кремы, тональники',
    price: 0,
    icon: '💅',
    image: '',  // НЕТ картинки
    visible: true
});
location.reload();
\`\`\`

### Пример 3: Скрытый товар (не показывать на сайте)

\`\`\`javascript
DataManager.addProduct({
    name: 'Акции',
    category: 'promo',
    description: 'Скидки до 50%',
    price: 0,
    icon: '🎁',
    image: '',
    visible: false  // НЕ показывать на сайте
});
location.reload();
\`\`\`

### Пример 4: Добавить несколько товаров сразу

\`\`\`javascript
// Массив товаров
const newProducts = [
    {
        name: 'Массажные масла',
        category: 'massage',
        description: 'Расслабляющие масла',
        price: 0,
        icon: '🫧',
        image: 'images/massage.jpg',
        visible: true
    },
    {
        name: 'Скрабы для тела',
        category: 'scrubs',
        description: 'Отшелушивание',
        price: 0,
        icon: '🧽',
        image: 'images/scrubs.jpg',
        visible: true
    },
    {
        name: 'Бальзамы для губ',
        category: 'lipcare',
        description: 'Увлажнение и защита',
        price: 0,
        icon: '💋',
        image: '',
        visible: true
    }
];

// Добавляем все товары
newProducts.forEach(product => DataManager.addProduct(product));

// Перезагружаем страницу
location.reload();
\`\`\`

---

## 🛠️ Способ 2: Редактирование кода (для разработчиков)

### Если вы хотите задать товары по умолчанию в коде

Откройте файл `js/data.js` и найдите функцию `getDefaultProducts()` (строка 343):

\`\`\`javascript
getDefaultProducts() {
    return [
        // Существующие товары...
        {
            id: this.generateId(),
            name: 'Декоративная косметика',
            category: 'makeup',
            description: 'Помады, тени, тушь',
            price: 0,
            icon: '💄',
            image: '',
            visible: true,
            createdAt: new Date().toISOString(),
            updatedAt: new Date().toISOString()
        },

        // ✅ ДОБАВЬТЕ НОВЫЙ ТОВАР СЮДА:
        {
            id: this.generateId(),
            name: 'ВАШ НОВЫЙ ТОВАР',
            category: 'your_category',
            description: 'Ваше описание',
            price: 0,
            icon: '✨',
            image: 'images/your_image.jpg',
            visible: true,
            createdAt: new Date().toISOString(),
            updatedAt: new Date().toISOString()
        }
    ];
}
\`\`\`

**Важно:** После редактирования:
1. Сохраните файл
2. Очистите LocalStorage в браузере:
   \`\`\`javascript
   DataManager.clearAll();
   location.reload();
   \`\`\`
3. Запушьте изменения в GitHub:
   \`\`\`bash
   git add js/data.js
   git commit -m "Add: New product to default catalog"
   git push origin main
   \`\`\`

---

## 🔍 Просмотр всех товаров

### Посмотреть список всех товаров

В консоли браузера (F12):

\`\`\`javascript
// Показать все товары
console.table(DataManager.getProducts());
\`\`\`

### Найти товар по ID

\`\`\`javascript
// Замените 'ID_ТОВАРА' на реальный ID
const product = DataManager.getProduct('ID_ТОВАРА');
console.log(product);
\`\`\`

---

## ✏️ Редактирование товара

### Изменить существующий товар

1. Узнайте ID товара (см. выше)
2. Обновите нужные поля:

\`\`\`javascript
// Пример: Изменить название и добавить картинку
DataManager.updateProduct('ID_ТОВАРА', {
    name: 'Новое название',
    image: 'images/new_image.jpg',
    description: 'Новое описание'
});

location.reload();
\`\`\`

### Скрыть товар с сайта

\`\`\`javascript
DataManager.updateProduct('ID_ТОВАРА', {
    visible: false
});
location.reload();
\`\`\`

### Показать скрытый товар

\`\`\`javascript
DataManager.updateProduct('ID_ТОВАРА', {
    visible: true
});
location.reload();
\`\`\`

---

## 🗑️ Удаление товара

### Удалить товар навсегда

\`\`\`javascript
// Удалить товар по ID
DataManager.deleteProduct('ID_ТОВАРА');
location.reload();
\`\`\`

### Удалить ВСЕ товары и сбросить к начальным

⚠️ **ОСТОРОЖНО! Это удалит все ваши товары!**

\`\`\`javascript
DataManager.clearAll();
location.reload();
\`\`\`

---

## 🎭 Категории товаров

Используйте эти названия категорий (латиницей):

| Категория | Название | Пример эмодзи |
|-----------|----------|---------------|
| `makeup` | Декоративная косметика | 💄 |
| `skincare` | Уход за кожей | 🧴 |
| `cleansing` | Очищение | 🧼 |
| `nails` | Маникюр | 💅 |
| `perfume` | Парфюмерия | 🌸 |
| `haircare` | Уход за волосами | 💆‍♀️ |
| `shaving` | Бритьё | 🪒 |
| `suncare` | Солнцезащита | ☀️ |
| `bodycare` | Уход за телом | 🫧 |
| `lipcare` | Уход за губами | 💋 |

**Можно создавать свои категории** - просто напишите название латиницей без пробелов.

---

## 📤 Экспорт и импорт товаров

### Экспортировать все товары в файл

\`\`\`javascript
// Создать JSON файл со всеми товарами
const data = DataManager.exportData();
const blob = new Blob([data], { type: 'application/json' });
const url = URL.createObjectURL(blob);
const a = document.createElement('a');
a.href = url;
a.download = 'optstock_products_' + new Date().toISOString().slice(0,10) + '.json';
a.click();
\`\`\`

### Импортировать товары из файла

\`\`\`javascript
// 1. Вставьте JSON данные в переменную
const jsonData = \`ВСТАВЬТЕ_СЮДА_JSON_ИЗ_ФАЙЛА\`;

// 2. Импортируйте
DataManager.importData(jsonData);

// 3. Перезагрузите страницу
location.reload();
\`\`\`

---

## 🚨 FAQ и решение проблем

### ❓ Товар не появляется на сайте

**Проверьте:**
1. ✅ `visible: true` (не `false`)
2. ✅ Обновили страницу (F5 или `location.reload()`)
3. ✅ Нет ошибок в консоли (F12 → Console)

### ❓ Картинка не отображается

**Проверьте:**
1. ✅ Файл лежит в папке `images/`
2. ✅ Путь указан правильно: `images/filename.jpg` (не `/images/` и не `./images/`)
3. ✅ Расширение файла написано правильно (`.jpg`, `.png`, `.webp`)
4. ✅ Имя файла без русских букв и пробелов

### ❓ Как изменить порядок товаров?

Товары показываются в порядке добавления. Чтобы изменить порядок:

\`\`\`javascript
// 1. Получить все товары
const products = DataManager.getProducts();

// 2. Поменять местами (пример: 1-й и 3-й товар)
[products[0], products[2]] = [products[2], products[0]];

// 3. Сохранить
DataManager.setData('optstock_products', products);

// 4. Обновить страницу
location.reload();
\`\`\`

### ❓ Данные исчезли после очистки кэша

**Причина:** Все товары хранятся в LocalStorage браузера (не на сервере).

**Решение:**
1. Регулярно делайте экспорт данных (см. раздел "Экспорт/Импорт")
2. Или добавляйте товары в код (`data.js → getDefaultProducts()`)

### ❓ Как добавить цену к товару?

Измените поле `price`:

\`\`\`javascript
DataManager.updateProduct('ID_ТОВАРА', {
    price: 2500  // Цена в вашей валюте
});
location.reload();
\`\`\`

Чтобы показывать цены на сайте, нужно изменить код в `main.js` (требуется разработчик).

### ❓ Сколько товаров можно добавить?

Технически - неограниченно. Но LocalStorage имеет лимит ~5-10 МБ.

**Рекомендуется:**
- До 50 товаров без проблем
- Картинки загружайте в папку `images/` (не в LocalStorage)
- Для больших каталогов (>100 товаров) нужна база данных (требуется разработчик)

---

## 🎓 Полезные команды

| Команда | Описание |
|---------|----------|
| `DataManager.getProducts()` | Список всех товаров |
| `DataManager.addProduct({...})` | Добавить новый товар |
| `DataManager.updateProduct(id, {...})` | Изменить товар |
| `DataManager.deleteProduct(id)` | Удалить товар |
| `DataManager.clearAll()` | ⚠️ Удалить ВСЁ |
| `location.reload()` | Перезагрузить страницу |
| `console.table(DataManager.getProducts())` | Красиво показать таблицу товаров |

---

## 📞 Поддержка

Если возникли вопросы или ошибки:
1. Откройте консоль (F12) и посмотрите на ошибки
2. Сделайте скриншот ошибки
3. Свяжитесь с разработчиком

---

## ✨ Советы

1. **Делайте резервные копии** регулярно (экспорт данных)
2. **Используйте эмодзи** вместо картинок для быстрого старта
3. **Картинки оптимизируйте** перед загрузкой (до 100-200 КБ)
4. **Не храните большие изображения** в LocalStorage
5. **Тестируйте на мобильных** устройствах после добавления товаров

---

Создано для проекта **OPT STOCK** 💄✨
